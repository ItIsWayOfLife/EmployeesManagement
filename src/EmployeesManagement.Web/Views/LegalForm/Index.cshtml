@model IEnumerable<EmployeesManagement.Web.Models.LegalForm.LegalFormViewModel>

@{
    ViewBag.Title = "Организационно-правовые формы";
}

<div class="row mb-5 mt-5">
    <div class="col-12 shadow-lg p-0 bg-white">
        <div class="card">
            <div class="card-header">
                <h3>Организационно-правовые формы</h3>
            </div>
            <div class="card-body">
                <div class="container">
                    @if (Model.Any())
                    {
                        <table class="table">
                            <tr>
                                <th>
                                    Id
                                </th>
                                <th>
                                    Название
                                </th>
                            </tr>
                            @foreach (var legalForm in Model)
                            {
                                <tr>
                                    <td>@legalForm.Id</td>
                                    <td>@legalForm.Name</td>
                                </tr>
                            }
                        </table>
                    }
                    else
                    {
                        <h2 class="text-center text-danger">
                            Список пуст
                        </h2>
                    }
                </div>
            </div>
        </div>
    </div>
</div>
